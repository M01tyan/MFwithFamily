<%= "r2m " + Time.now.strftime('%Y%m%d') %>
サービス： account_web
PR_url： <%= "##{release_pull_request.pr.number}" %>
説明：
<% pull_requests.each do |pr| -%>
<%= "##{pr.pr.number} #{pr.pr.title} @#{pr.pr.user.login}" %>
<% end -%>

stg確認はしましたか？
<% pull_requests.each do |pr| -%>
  <%= "##{pr.pr.number} #{pr.pr.title} @#{pr.pr.user.login}" %>
<% end -%>
